# Form
An application form that takes in an array of record(s): first & last name and address.

The inputs are placed in a binary file called 'dat' file (person.dat), then read back to array of records that will be stored into a 'txt' file (person.txt).
